import express from "express";
const router = express.Router();
import { registerUser } from "../controllers/userController.js";

// finish the registration and make sure it works. FInish the model add the bcrypt,
//maybe in the actual registraion route in the userController. Proshop has it
// on the model but dev has it on the route itself. decide whcih is better.
